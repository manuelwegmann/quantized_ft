# Install with:
#   pip install torch==2.10.0+cu128 torchvision==0.25.0+cu128 \
#       --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt

# CT volume I/O
nibabel>=4.0

# Data handling
pandas>=1.5
numpy>=1.23
openpyxl>=3.0

# Progress bars
tqdm>=4.64

# Evaluation
scikit-learn>=1.2

# Config
pyyaml>=6.0

# CT-CLIP / CTViT dependencies (added to sys.path, not pip-installed as packages)
einops>=0.7
vector_quantize_pytorch>=1.0
transformers>=4.30
beartype>=0.15
ema_pytorch>=0.4
accelerate>=0.20
opencv-python-headless>=4.5   # pulled in by transformer_maskgit trainer imports
